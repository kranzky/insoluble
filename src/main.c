#include "dictionary.h"
#include "tokenizer.h"

#include <stdlib.h>
#include <stdio.h>

//==============================================================================

// process arguments
// read from utf-8 file and write to utf-8 file
// support multiple models

int main(int argc, char **argv)
{
  int retval = EXIT_SUCCESS;
  fprintf(stderr, "Insoluble says hi.\n");
  return retval;
}
